# --- Core Data Science & Math (All Questions) ---
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Q1: Time-Series Forecasting (LSTM/GRU) & Q3: Deep Learning ---
# TensorFlow is used for building LSTM/GRU models and custom neural networks.
tensorflow>=2.13.0
# Scikit-learn is needed for preprocessing (scaling) and metrics (MSE).
scikit-learn>=1.3.0

# --- Q2: Reinforcement Learning ---
# Gymnasium is the standard API for RL environments.
gymnasium>=0.29.0

# --- Q3: Adversarial Robustness ---
# (Optional) Specialized library for generating adversarial attacks/defenses.
adversarial-robustness-toolbox>=1.15.0

# --- Q4: Fairness & Bias Mitigation ---
# Fairlearn provides metrics (like Equalized Odds) and bias mitigation algorithms.
fairlearn>=0.9.0
